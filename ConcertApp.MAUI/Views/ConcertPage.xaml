<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ConcertApp.MAUI.Views.ConcertPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ConcertApp.MAUI.Views"
    Title="Available Concerts">

    <VerticalStackLayout Padding="20">
        <Label Text="Select a Concert" 
               FontSize="24" 
               FontAttributes="Bold" 
                HorizontalOptions="Center" />
        <Button Text="Logout" Command="{Binding LogoutCommand}" />
        <CollectionView ItemsSource="{Binding Concert}" SelectionMode="Single" 
                        SelectedItem="{Binding SelectedConcert, Mode=TwoWay}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" Margin="5">
                        <Grid>
                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold"/>
                            <Label Text="{Binding Description}" FontSize="14" TextColor="Gray"/>
                        </Grid>
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type local:ConcertPage}}, Path=BindingContext.NavigateToPerformancesCommand}"
                                                  CommandParameter="{Binding Id}" />
                        </Frame.GestureRecognizers>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
      </VerticalStackLayout>




</ContentPage>